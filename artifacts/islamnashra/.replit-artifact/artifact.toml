kind = "mobile"
previewPath = "/"
title = "Digital X News"
version = "1.0.0"
id = "artifacts/islamnashra"
router = "expo-domain"

[[services]]
name = "expo"
ensurePreviewReachable = "/status"
paths = [ "/" ]
localPort = 20173

[services.development]
run = "pnpm --filter @workspace/digital-x-news run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/digital-x-news", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/digital-x-news", "run", "serve" ]

[services.env]
PORT = "20173"
BASE_PATH = "/"
